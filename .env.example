# Copy this file to ".env" and fill in your real values. NEVER commit the real .env.
# MongoDB Atlas connection string (free M0 tier):
MONGODB_URI="mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority"
MONGO_DB="predictive_maintenance"
MONGO_COLLECTION="sensor_readings"
SEED_CSV_PATH="./data/raw_data/predictive_maintenance_dataset.csv"
